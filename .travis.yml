language: julia
os:
  - linux
  - osx
julia:
  - 0.6
# - nightly
notifications:
  email: false
script:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  - julia -e 'Pkg.clone("https://github.com/ZKSI/SymmetricTensors.jl"); Pkg.build("SymmetricTensors"); Pkg.checkout("SymmetricTensors", "nonullablearrays")'
  - julia -e 'Pkg.test("SymmetricTensors"; coverage=true)'
after_success:
  - julia -e 'cd(Pkg.dir("SymmetricTensors")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(process_folder())'
